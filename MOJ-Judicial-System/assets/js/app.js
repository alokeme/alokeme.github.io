$(function(){const box=$('[data-live-search]');if(box.length){let t;box.on('input',function(){clearTimeout(t);const q=this.value;t=setTimeout(()=>{$.get('../api/search.php',{q},html=>$('#results').html(html.items?html.items.map(i=>`<div class="card result-card my-3"><div class="card-body"><a href="decision.php?id=${i.id}"><h5>${i.title}</h5></a><p>${i.summary??''}</p></div></div>`).join(''):'لا توجد نتائج'),'json');},300);});}});
